# Training-only dependencies. Install into the project venv with:
#   pip install -r requirements.txt -r requirements-training.txt
# torch: PyPI Windows wheels are CPU builds; for CUDA install from pytorch.org.
torch>=2.2
transformers>=4.40
scikit-learn>=1.4
tqdm>=4.66
